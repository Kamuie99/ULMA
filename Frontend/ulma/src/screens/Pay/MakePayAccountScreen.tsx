import React from 'react';
import {StyleSheet, View} from 'react-native';

interface MakePayAccountScreenProps {}

function MakePayAccountScreen({}: MakePayAccountScreenProps) {
  return <View></View>;
}

const styles = StyleSheet.create({});

export default MakePayAccountScreen;
